# term

A basic terminal emulator built in rust using wgpu & glyphon for text rendering and termial emulation from scratch
![](/example_image.png)

### warning:

* since it does not support much escape codes things like vim and commands like clear will not work currently
* hopefully in the future I will implement this